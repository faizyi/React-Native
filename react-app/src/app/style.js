import { StyleSheet } from "react-native"
export default styles = StyleSheet.create({
    container : {
        // marginTop: 20,
        display : "flex",
        justifyContent : "center",
        alignItems : "center",
    }
})